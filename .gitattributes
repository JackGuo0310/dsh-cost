# Normalize every text file to LF in the repository and in the working tree.
# The client bundle is served verbatim to the browser and the installer copies it
# byte-for-byte, so keeping one line ending everywhere avoids churn across
# Windows/macOS/Linux checkouts.
* text=auto eol=lf
